using UnityEngine;
using System.Collections;

public class SyntaxAndConvetions : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//this line of code will tell me where the box is
		Debug.Log (transform.position.x);
	/*
		This would be used to make a paragraph of pesdou code.
	*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
